apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "keycloak.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "keycloak.labels" . | nindent 4 }}
spec:
  secretName: {{ include "keycloak.certificateSecretName" . }}
  issuerRef:
    name: eaistack-ca-issuer
    kind: ClusterIssuer
  dnsNames:
  # In-cluster service DNS names (Decision 1: both short and fully-qualified)
  - {{ include "keycloak.fullname" . }}
  - {{ include "keycloak.fullname" . }}.{{ .Release.Namespace }}.svc
  - {{ include "keycloak.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
  {{- if .Values.externalHostname }}
  - {{ .Values.externalHostname }}
  {{- end }}
